version: "2"
services:
  app:
    container_name: app
    build: .
    ports:
      - "8000:8080"
    links:
      - csmpdbserver

  csmpdbserver:
    container_name: csmpdbserver
    image: mongo
    ports:
      - "27017:27017"

